services:
  - type: web
    name: code-review-app
    env: node
    plan: free
    buildCommand: chmod +x .render-build.sh && ./.render-build.sh
    startCommand: cd /opt/render/project/src && NODE_ENV=production npm start
